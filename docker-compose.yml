version: "3"

services:
  gunicorn:
    container_name: gunicorn
    build:
      dockerfile: Gunicorn.Dockerfile

  nginx:
    build:
      dockerfile: Nginx.Dockerfile
    networks:
      - default
      - nginx

networks:
  default:
  nginx:
    external: true
